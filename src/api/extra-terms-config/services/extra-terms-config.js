'use strict';

/**
 * extra-terms-config service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::extra-terms-config.extra-terms-config');
