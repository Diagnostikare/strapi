'use strict';

/**
 * extra-terms-config router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::extra-terms-config.extra-terms-config');
