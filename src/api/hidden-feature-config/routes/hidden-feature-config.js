'use strict';

/**
 * hidden-feature-config router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::hidden-feature-config.hidden-feature-config');
